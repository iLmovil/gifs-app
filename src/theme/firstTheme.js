import { createTheme } from "@mui/material";

export const firstTheme = createTheme({
    palette: {
        primary: {
            main: '#22577a'
        },
        secondary: {
            main: '#e0fbfc'
        },
        light: {
            main: '#edf2f4'
        }
    }
})